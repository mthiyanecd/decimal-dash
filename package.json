{
  "name": "decimal-dash-go-live-contracts",
  "private": true,
  "type": "module",
  "scripts": {
    "test:contracts": "node tests/run-all.mjs",
    "test:rules": "firebase emulators:exec --project demo-study-dash --only firestore \"node tests/firestore-rules.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0",
    "firebase-tools": "15.24.0"
  }
}
